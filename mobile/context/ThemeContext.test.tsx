import {
  render,
} from "@testing-library/react-native";

describe(
  "ThemeProvider",
  () => {

    it(
      "renders correctly",
      () => {
        expect(true).toBe(true);
      }
    );
  }
);